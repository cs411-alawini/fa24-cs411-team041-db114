import React from 'react';

// 注意从本地存储获取当前id!!! const user_id = localStorage.getItem('user_id');


const FavoriteJobs = () => {
  return (
    <div>
      <h1>Favorite Jobs Page</h1>
    </div>
  );
};

export default FavoriteJobs; 